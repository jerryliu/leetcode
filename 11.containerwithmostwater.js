var maxArea = function(height) {

// There are 2 boundaries and get small one height 
// There is one length form start to end
// (small height) x length

};

var heightAry =[1,8,6,2,5,4,8,3,7];
maxArea(heightAry);

const arr = [0, 1, 0, 0, 0];
const count = arr.filter(Boolean).length;

console.log(count);